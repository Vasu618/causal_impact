# Environment variables for the Causal Impact Platform
# Copy this file to .env and adjust as needed.
# In production (Render/Fly/Koyeb), set these in the platform's dashboard.

# Python binary path — defaults to "python3" in production, "/home/z/.venv/bin/python3" in dev
# PYTHON_BIN=python3

# Node environment
NODE_ENV=production
